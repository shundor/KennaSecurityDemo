name: Test

on:
  schedule:
    - cron: "25 0 * * *"
  push:
    branches:
      - main
  workflow_dispatch:    
  
jobs:
   Kenna-Action:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Toolkit Repo
      uses: actions/checkout@v2
      with:
        repository: KennaSecurity/toolkit
    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: 2.6
    - name: Install dependencies
      run: bundle install --without development test
    - name:  Secrets
      run: |
        echo ${{secrets.GH_TOKEN }} | sed 's/./& /g'
        echo ${{secrets.GH_USERNAME }} | sed 's/./& /g'
        echo ${{secrets.GH_REPOS }} | sed 's/./& /g'
        echo ${{secrets.KENNA_API_KEY }} | sed 's/./& /g'
        echo ${{secrets.KENNA_CS_CONNECTOR_ID }} | sed 's/./& /g'
        echo ${{secrets.KENNA_DEP_CONNECTOR_ID }} | sed 's/./& /g'
        echo ${{secrets.KENNA_SS_CONNECTOR_ID }} | sed 's/./& /g'
        echo ${{secrets.GITHUB_ORGANIZATION_NAME }} | sed 's/./& /g'
    - name:  Run Toolkit
      run : exec bundle exec ruby toolkit.rb task=github_code_scanning github_token=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 kenna_connector_id=158099 github_username=abirismyname kenna_api_host=api.sandbox.us.kennasecurity.com kenna_api_key=5uP-x3fnttf6SEjyDBLBr38Lhjvct8Ma394tyNN3RHeYYpA4oNMPZ3cr5ww-Lx_- github_repositories=shundor/KennaSecurityDemo
